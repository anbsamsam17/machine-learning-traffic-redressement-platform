import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "MDL Redressement — Extrapolation",
};

export default function ExtrapolationLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
